for (int i = 2; i < m + 1; i++) {
    //     if (b[x][i] >= b[x][i - 1]) {
    //         L[i] = L[i - 1];
    //     } else {
    //         if (b[x][i] <= L[i - 1]) {
    //             L[i] = b[x][i];
    //         } else {
    //             L[i] = L[i - 1];
    //         }
    //     }
    // }

    // R[m] = b[x][m];
    // for (int i = m - 1; i > 0; i++) {
    //     if (b[x][i] >= b[x][i + 1]) {
    //         R[i] = R[i + 1];
    //     } else {
    //         if (b[x][i] <= R[i + 1]) {
    //             R[i] = b[x][i];
    //         } else {
    //             R[i] = R[i + 1];
    //         }
    //     }
    // }